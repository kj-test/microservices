server:
  port: 8084
spring:
  application:
    name: ms-mail
  cloud:
    config:
      discovery:
        enabled: true
        service-id: ms-config-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true        